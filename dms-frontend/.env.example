# DMS Frontend Environment Variables
# Copy to .env.local for development

# API base URL — empty in dev (Vite proxy handles /v1/* -> localhost:4000)
VITE_API_BASE_URL=

# Production example:
# VITE_API_BASE_URL=https://api.yourdomain.com
